endpoints:
  health:
    sensitive: true
management:
  security:
    enabled: false
  endpoint:
    gateway:
      access: unrestricted
    health:
      show-details: always
      show-components: always
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: prometheus, metrics, info, health, shutdown, beans, env, configprops, gateway
